"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeManifest = void 0;
const fs_1 = __importDefault(require("fs"));
async function writeManifest(path, entries, author) {
    return new Promise((resolve, reject) => {
        fs_1.default.promises.writeFile(`${path}/fxmanifest.lua`, `fx_version "cerulean"
      games { 'rdr3', 'gta5' }
      description 'This manifest was generated by fxmanifestmaker by LedAndris'
      version '1.0.0'
      author '${author}'

      ui_page ${entries.UIP[0].replace(",", "")}
      client_scripts {
        ${entries.CL.join("\n")}}

      server_scripts {
          ${entries.SV.join("\n")}}

      shared_scripts {
            ${entries.SH.join("\n")}}

      files {
              ${entries.FILE.join("\n")}}
              
      ${entries.DATA_FILES.join("\n")}

      `)
            .then(() => {
            resolve();
        });
    });
}
exports.writeManifest = writeManifest;
//# sourceMappingURL=builder.js.map